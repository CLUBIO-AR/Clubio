# REVIEW CONSOLIDADO — CLUBIO — Full Scan (tercera pasada)
**Fecha:** 2026-06-15 ~02:00
**Branch:** main
**Reviewers ejecutados:** security | architecture | qa

---

## Resumen Ejecutivo
**Veredicto:** APROBADO CON OBSERVACIONES ✅⚠️
**Nivel de riesgo:** BAJO
**Total hallazgos:** 0 críticos, 0 altos, 3 medios, 5 bajos

El codebase está en buen estado. Todos los críticos y altos de sesiones anteriores fueron resueltos.
Los tres hallazgos medios son independientes y ninguno bloquea producción.

---

## Hallazgos Consolidados

### 🔴 Críticos — Ninguno

### 🟠 Altos — Ninguno

### 🟡 Medios (deuda técnica a registrar)

| # | Área | Descripción | Fuente | Archivo |
|---|------|-------------|--------|---------|
| 1 | Integridad de pago | `from-redirect` no verifica `payment.external_reference === cuota_id`. Ventana de race condition: si from-redirect dispara antes que el webhook, un tercero con ambos IDs podría atribuir el pago a otra cuota | security + qa | `app/api/webhooks/mercadopago/from-redirect/route.ts:44` |
| 2 | Email remitente | Worker de avisos lote no incluye `email_remitente_nombre`/`email_remitente_address` en la query de gymConfig. El remitente personalizado configurado por el gym nunca se aplica en avisos consolidados | architecture + qa | `app/api/cron/workers/enviar-avisos-gym/route.ts:30-32` |
| 3 | Double-submit admin | `POST /api/cuotas/pagar-lote` no incluye `cuota_hash` en el INSERT → la protección del migration 0016 no cubre este path autenticado | architecture + qa | `app/api/cuotas/pagar-lote/route.ts:72-80` |

### 🔵 Bajos / Sugerencias

| # | Área | Descripción | Fuente | Archivo |
|---|------|-------------|--------|---------|
| 4 | Auth consistency | `pagos/verificar/route.ts` no usa `getApiGymContext()` → gym suspendido puede verificar pagos manualmente | security | `app/api/pagos/verificar/route.ts:16-22` |
| 5 | Soft-delete | `crearTenantAction` hace DELETEs físicos en rollback de creación fallida (registros nunca activos) | architecture | `app/actions/admin-nuevo-gym.ts:175-183` |
| 6 | N+1 query | Update de `avisos_enviados` en loop secuencial en path multi-cuota del worker | architecture | `app/api/cron/workers/enviar-avisos-gym/route.ts:209-213` |
| 7 | Migration numbering | Dos migrations con prefijo `0008_` — no determinístico con `supabase db push` | qa | `supabase/migrations/` |
| 8 | Tests vacíos | `__tests__/integration/api` y `integration/crons` sin tests; flujos críticos de pago sin cobertura | qa | `__tests__/` |

---

## Fixes recomendados para los Medios

### #1 — from-redirect external_reference check
```typescript
// app/api/webhooks/mercadopago/from-redirect/route.ts
// Después de: if (payment.status !== "approved") ...
if (payment.external_reference !== cuota_id) {
  return NextResponse.json({ ok: false, error: "external_reference mismatch" }, { status: 422 });
}
```

### #2 — enviar-avisos-gym gymConfig query
```typescript
// Línea 31 — agregar los campos faltantes al SELECT
const { data: gymConfig } = await admin
  .from("gym_config")
  .select("email_activo, whatsapp_activo, whatsapp_phone_number_id, whatsapp_access_token, email_color_acento, email_templates, email_remitente_nombre, email_remitente_address")
  .eq("gym_id", gym_id)
  .single();

// Línea 68 — agregar al notifConfig
const notifConfig: GymNotificationConfig = {
  email_activo: gymConfig.email_activo ?? true,
  email_remitente_nombre: gymConfig.email_remitente_nombre ?? null,
  email_remitente_address: gymConfig.email_remitente_address ?? null,
  // ... resto igual
};
```

### #3 — pagar-lote con cuota_hash
```typescript
// Igual que en pagar/lote/[token]/page.tsx
import { createHash } from "crypto";
const cuotaHash = createHash("sha256")
  .update(`${cuotas[0].alumno_id}:${[...cuota_ids].sort().join(",")}`)
  .digest("hex");

// En el INSERT:
.insert({ ..., cuota_hash: cuotaHash })
```

---

## Controles verificados como OK

| Control | Estado |
|---------|--------|
| Webhooks MP (main + suscripciones): HMAC-SHA256 | ✅ |
| JWT en /pagar y /pagar/lote: firma + exp + jti | ✅ |
| getApiGymContext(): verifica gym.activo | ✅ |
| Crons: CRON_SECRET en todos los dispatchers y workers | ✅ |
| Resend: única importación en lib/notifications/channels/email.ts | ✅ |
| Multi-tenancy: gym_id explícito en todas las queries | ✅ |
| Input validation: Zod en todos los endpoints | ✅ |
| Debug endpoints: ninguno | ✅ |
| pagos table: solo INSERT (inmutable) | ✅ |
| Soft delete en alumnos: softDeleteAlumno() confirmado | ✅ |
| Admin panel: requireSuperadmin() en layouts y todas las actions | ✅ |
| register-gym: rollback atómico vía RPC crear_gym_completo | ✅ |
| METODO_LABEL: centralizado en lib/constants.ts | ✅ |
| cuota_lotes: cuota_hash + índice UNIQUE parcial (migration 0016) | ✅ |
| /api/leads: rate limit por email (10 min) | ✅ |

---

## Decisión Final

> **APROBADO CON OBSERVACIONES** — Sin críticos ni altos. Tres medios con fix conocido; el más importante (#1, from-redirect) resuelve con una sola línea de verificación.

**Aceptado conscientemente:**
- Hallazgo #5 (DELETE en rollback de admin-nuevo-gym): registros nunca activos, creación rollback legítimo.
- Hallazgo #8 (tests vacíos): deuda técnica conocida, no regresión nueva en esta sesión.
- Hallazgo #7 (migration numbering): impacto solo en CLI automático; aplicación manual no afectada.
